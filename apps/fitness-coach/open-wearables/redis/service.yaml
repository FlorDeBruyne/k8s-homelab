apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: open-wearables
spec:
  selector:
    app: redis-service
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: open-wearables
spec:
  selector:
    app: frontend-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP